/*
Identify flaws in the essay.

Reply with:
`true` if the prompt requires additional context to complete
`false` if the prompt does not require additional context

Reply with exactly `true` or `false` lowercase
*/